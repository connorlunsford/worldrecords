<h3>Insert Data Into "Vendors"</h3>
<p>Business use only. These are vendors that the store purchases products from.
    Vendor ID (vid) auto_increments. 
    When inserting a new product under "All Products", check this table to get the appropriate vid.
</p>
<form action="flip3.engr.oregonstate.edu:7755/vendors_insert" method="GET">
    <label for="company">Company Name: </label>
    <input type="text" id="company"><br>
    <label for="email">Email: </label>
    <input type="text" id="email"><br>
    <label for="phone">Phone Number: </label>
    <input type="text" id="phone"><br>
    <input type="submit" id="vendors">
</form>


<div>
    <table>
        <header>
            <tr id="head">
                <th>Vendor ID</th>
                <th>Company</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Delete</th>
            </tr>
        </header>
        <tbody id="table">
            {{#each results}}
            <tr>
                <td>{{this.vid}}</td>
                <td>{{this.company}}</td>
                <td>{{this.email}}</td>
                <td>{{this.phone}}</td>
                <td><button type="button" class="delete_button">Delete</button></td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>

{{!-- The script is pulled from public/js/vendors_ajax.js 
WR-Server-Side.js (line 14) specifies that all source files are pulled from public folder.--}}
<script src="js/vendors_ajax.js"></script>
